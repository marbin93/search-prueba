-- crear una nueva rama 
git branch nueva-rama

--Cambiarce de rama
git checkout nombre_rama

-- subir cambios
git status

git add .

git commit -m "comentario" 

git push rama_cambio

-- Combinar rama 

